import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    bottom: 0,
    left: 20,
    right: 20,
  },
  snackbar: {
    borderRadius: 8,
  },
});

export default styles;
